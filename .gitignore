# ===== Kanzi Studio: 本地/自动生成文件,不应提交 =====

# 自动保存(Kanzi Studio autosave)
*.kzproj.autosave
*.kzproj.autosave.user

# 用户本地设置(每人不同)
*.kzproj.user

# Kanzi Studio 滚动备份(Car.kzproj_1 / _2 ...)
*.kzproj_*

# 工程会话锁文件
*.lock

# Kanzi 临时 / 备份目录(自动生成)
**/Temp/
**/Backup/

# ===== 自动生成的图片/导出缓存(Kanzi 打开/导入/导出时自动重建)=====
# 用 **/ 以匹配任意层级(如 IVI/car/Images/...)
**/Images/thumbnails/
**/Images/mipmaps/
**/Images/compressed/
**/Images/cubemaps/mipmaps/
**/Images/cubemaps/thumbnails/
**/Images/cubemaps/compressed/
# 导出缓存文件(任意目录)
*.KZBExport.cache

# ===== 导出/构建产物(导出时重新生成)=====
# 注意:数据源 XML 等源文件不在此列(*.xml 不被忽略)
*.kzb
*.kzb.cfg
*.kzb.txt

# ===== OS / 编辑器 =====
.DS_Store
Thumbs.db
*.swp
*~

# 大资源(3D / 高清纹理 / 字体)建议用 Git LFS,按需在 .gitattributes 配置
