# Kanzi 导出产物(由 CI 生成并归档,不进 Git)
*.kzb
*.kzb.cfg
out/
output/

# kzb-only 工作流:每个工程只跟踪 Tool_project/,不需要 C++ Application 脚手架
# (Studio 新建工程会默认生成 Application/,这里统一忽略,本地保留即可)
studio-projects/**/Application/

# Kanzi Studio 临时 / 缓存
*.tmp
.kanzi_cache/
**/Backup/
**/Temp/

# OS / 编辑器
.DS_Store
Thumbs.db
*.swp

# 大资源建议用 Git LFS 管理(3D / 高清纹理 / 字体),按需在 .gitattributes 配置
